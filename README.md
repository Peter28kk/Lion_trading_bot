# Lion_trading_bot

A Python-based trading bot ready for deployment on Railway.

## Setup

1. Install dependencies:

2. Create a `.env` file based on `.env.example` and add your API keys.

3. Run the bot:

## Deployment

This bot is ready for Railway deployment using a Dockerfile or Procfile.

